{
  "$defs": {
    "EnumBindingOrderDirection": {
      "description": "Direction the upstream projection orders its rows by.",
      "enum": [
        "ascending",
        "descending"
      ],
      "title": "EnumBindingOrderDirection",
      "type": "string"
    },
    "EnumCommitLevel": {
      "description": "Durability of the effect a UI action commits, lowest to highest.",
      "enum": [
        "read_only",
        "reversible",
        "irreversible"
      ],
      "title": "EnumCommitLevel",
      "type": "string"
    },
    "EnumEmptyStateReason": {
      "description": "Typed reason a UI component renders an empty/error state.\n\nMirrors the ``EmptyStateReason`` TS union exactly (four values, no more).\nA renderer MUST NOT collapse ``SCHEMA_INVALID`` into ``NO_DATA`` \u2014 each\nreason maps to a distinct operator diagnostic.",
      "enum": [
        "no-data",
        "missing-field",
        "upstream-blocked",
        "schema-invalid"
      ],
      "title": "EnumEmptyStateReason",
      "type": "string"
    },
    "EnumEvidenceGateMoment": {
      "description": "The moment at which an evidence requirement is enforced.",
      "enum": [
        "on-render",
        "on-commit"
      ],
      "title": "EnumEvidenceGateMoment",
      "type": "string"
    },
    "EnumEvidenceKind": {
      "description": "Types of evidence required for ticket contract validation.\n\nEvidence kinds specify what type of proof is required:\n- TESTS: Automated test coverage\n- DOCS: Documentation updates\n- CI: CI/CD pipeline changes\n- BENCHMARK: Performance benchmarks\n- MANUAL: Manual verification steps",
      "enum": [
        "tests",
        "docs",
        "ci",
        "benchmark",
        "manual"
      ],
      "title": "EnumEvidenceKind",
      "type": "string"
    },
    "EnumGateKind": {
      "description": "Types of gates that require approval.",
      "enum": [
        "human_approval",
        "policy_check",
        "security_check"
      ],
      "title": "EnumGateKind",
      "type": "string"
    },
    "EnumRiskLevel": {
      "description": "Risk level of executing a UI action, lowest to highest.",
      "enum": [
        "low",
        "medium",
        "high",
        "critical"
      ],
      "title": "EnumRiskLevel",
      "type": "string"
    },
    "EnumTicketStepStatus": {
      "description": "Status values for ticket verification steps and gates.",
      "enum": [
        "pending",
        "passed",
        "failed",
        "skipped",
        "approved",
        "rejected"
      ],
      "title": "EnumTicketStepStatus",
      "type": "string"
    },
    "EnumWidgetType": {
      "description": "Dashboard widget type enumeration.\n\nDefines the types of widgets available for dashboard configuration.\nEach type has specific configuration requirements and rendering behavior.\nWidget types are categorized as either data-bound (requiring continuous\ndata updates) or aggregation-based (displaying point-in-time metrics).\n\nAttributes:\n    CHART: Line, bar, area, pie, or scatter chart visualization.\n        Config: :class:`~omnibase_core.models.dashboard.ModelWidgetConfigChart`\n    TABLE: Paginated, sortable tabular data display.\n        Config: :class:`~omnibase_core.models.dashboard.ModelWidgetConfigTable`\n    METRIC_CARD: Single KPI display with optional trend and thresholds.\n        Config: :class:`~omnibase_core.models.dashboard.ModelWidgetConfigMetricCard`\n    STATUS_GRID: Grid of status indicators for system health monitoring.\n        Config: :class:`~omnibase_core.models.dashboard.ModelWidgetConfigStatusGrid`\n    EVENT_FEED: Real-time event stream with filtering capabilities.\n        Config: :class:`~omnibase_core.models.dashboard.ModelWidgetConfigEventFeed`\n\nExample:\n    Use in widget definition::\n\n        from omnibase_core.enums import EnumWidgetType\n\n        # Check if widget needs real-time data binding\n        if EnumWidgetType.TABLE.is_data_bound:\n            setup_data_subscription()",
      "enum": [
        "chart",
        "table",
        "metric_card",
        "status_grid",
        "event_feed"
      ],
      "title": "EnumWidgetType",
      "type": "string"
    },
    "ModelActionContract": {
      "additionalProperties": false,
      "description": "Declares the command a UI action emits and its approval gate.\n\nThe action emits exactly one canonical command topic (a valid ONEX topic\nsuffix whose kind token is ``cmd``). When ``approval_gate`` is present the\naction requires that gate's approval before it commits; ``ModelGate`` owns\nthe approval semantics. ``correlation_required`` enforces that every emission\ncarries a correlation ID (Phase 1 bus trace depends on this).",
      "properties": {
        "action_id": {
          "description": "Stable semantic identifier for this action within a component",
          "minLength": 1,
          "title": "Action Id",
          "type": "string"
        },
        "command_topic": {
          "description": "Canonical onex.cmd.* topic this action emits onto the bus",
          "minLength": 1,
          "title": "Command Topic",
          "type": "string"
        },
        "label": {
          "description": "Human-readable label rendered on the action control",
          "minLength": 1,
          "title": "Label",
          "type": "string"
        },
        "approval_gate": {
          "anyOf": [
            {
              "$ref": "#/$defs/ModelGate"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Composed canonical approval gate; None means no approval required"
        },
        "gate_policy": {
          "anyOf": [
            {
              "$ref": "#/$defs/ModelActionGatePolicy"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Composed risk/confidence policy (confidence_threshold, requires_user_confirmation, risk_level, reversible, commit_level); None means no policy-driven gating beyond the approval gate"
        },
        "correlation_required": {
          "default": true,
          "description": "Whether every emission must carry a correlation ID",
          "title": "Correlation Required",
          "type": "boolean"
        }
      },
      "required": [
        "action_id",
        "command_topic",
        "label"
      ],
      "title": "ModelActionContract",
      "type": "object"
    },
    "ModelActionGatePolicy": {
      "additionalProperties": false,
      "description": "Risk/confidence policy a UI action gate enforces before committing.\n\nA UI action is a declared ``onex.cmd.*`` command emitter (a button). Before\nthat command is emitted, the action gate consults this policy to decide\nwhether to require user confirmation, how to render affordances/disabled\nstates, and what evidence the action must carry. The policy is declarative\nand platform-neutral; renderers derive behavior from it rather than encoding\nrisk semantics in frontend code.\n\nAttributes:\n    confidence_threshold: Minimum upstream confidence (0.0-1.0) required for\n        the action to proceed without escalation. Below this, the gate\n        escalates (e.g. forces confirmation regardless of other fields).\n    requires_user_confirmation: Whether the action gate must obtain explicit\n        user confirmation before the command is emitted.\n    risk_level: Typed user-facing risk of executing the action.\n    reversible: Whether the committed effect can be undone. This is the\n        boolean fast-path; ``commit_level`` carries the full typed ordinal.\n    commit_level: Typed durability of the effect the action commits\n        (read-only, reversible, or irreversible).",
      "properties": {
        "confidence_threshold": {
          "description": "Minimum upstream confidence (0.0-1.0) for the action to proceed without escalation",
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Confidence Threshold",
          "type": "number"
        },
        "requires_user_confirmation": {
          "description": "Whether explicit user confirmation is required before emit",
          "title": "Requires User Confirmation",
          "type": "boolean"
        },
        "risk_level": {
          "$ref": "#/$defs/EnumRiskLevel",
          "description": "Typed user-facing risk of executing the action"
        },
        "reversible": {
          "description": "Whether the committed effect can be undone",
          "title": "Reversible",
          "type": "boolean"
        },
        "commit_level": {
          "$ref": "#/$defs/EnumCommitLevel",
          "description": "Typed durability of the effect the action commits"
        }
      },
      "required": [
        "confidence_threshold",
        "requires_user_confirmation",
        "risk_level",
        "reversible",
        "commit_level"
      ],
      "title": "ModelActionGatePolicy",
      "type": "object"
    },
    "ModelDataBindingContract": {
      "additionalProperties": false,
      "description": "Declares how a component binds to a projection topic.\n\nThe binding names the projection topic, the ordering authority (the column\nthe projection orders by and the direction), and the required fields the\ncomponent consumes. Missing fields surface as a typed empty-state reason at\nrender time, never a fallback literal.",
      "properties": {
        "binding_id": {
          "description": "Stable semantic identifier for this binding within a component",
          "minLength": 1,
          "title": "Binding Id",
          "type": "string"
        },
        "projection_topic": {
          "description": "Canonical projection topic read via /projection/{topic}; no raw DB",
          "minLength": 1,
          "title": "Projection Topic",
          "type": "string"
        },
        "ordering_authority_field": {
          "description": "Column the upstream projection orders by (explicit ordering authority)",
          "minLength": 1,
          "title": "Ordering Authority Field",
          "type": "string"
        },
        "ordering_direction": {
          "$ref": "#/$defs/EnumBindingOrderDirection",
          "default": "descending",
          "description": "Direction the ordering-authority field is ordered by upstream"
        },
        "required_fields": {
          "default": [],
          "description": "Projection row fields this component requires to render",
          "items": {
            "type": "string"
          },
          "title": "Required Fields",
          "type": "array"
        },
        "cursor_field": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional pagination cursor field exposed by the projection",
          "title": "Cursor Field"
        }
      },
      "required": [
        "binding_id",
        "projection_topic",
        "ordering_authority_field"
      ],
      "title": "ModelDataBindingContract",
      "type": "object"
    },
    "ModelEvidenceRequirement": {
      "additionalProperties": false,
      "description": "Evidence requirement in ticket contract.\n\nDeclares what type of evidence must exist before a ticket can be marked Done.",
      "properties": {
        "kind": {
          "$ref": "#/$defs/EnumEvidenceKind",
          "description": "Type of evidence"
        },
        "description": {
          "description": "What evidence must exist",
          "maxLength": 10000,
          "title": "Description",
          "type": "string"
        },
        "command": {
          "anyOf": [
            {
              "maxLength": 10000,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How to reproduce, if applicable",
          "title": "Command"
        }
      },
      "required": [
        "kind",
        "description"
      ],
      "title": "ModelEvidenceRequirement",
      "type": "object"
    },
    "ModelEvidenceRequirementContract": {
      "additionalProperties": false,
      "description": "Declares evidence required before an action commits or a panel renders.\n\nComposes the canonical OCC ``ModelEvidenceRequirement`` rather than\nredefining its fields. ``gate_moment`` says whether the evidence gates a\nrender or a commit; ``unmet_display_message`` is the operator-facing message\nsurfaced when the requirement is not satisfied.",
      "properties": {
        "contract_id": {
          "description": "Stable semantic identifier for this evidence requirement contract",
          "minLength": 1,
          "title": "Contract Id",
          "type": "string"
        },
        "requirement": {
          "$ref": "#/$defs/ModelEvidenceRequirement",
          "description": "Canonical OCC evidence requirement composed into this UI contract"
        },
        "gate_moment": {
          "$ref": "#/$defs/EnumEvidenceGateMoment",
          "description": "Whether the evidence gates a panel render or an action commit"
        },
        "unmet_display_message": {
          "description": "Operator-facing message shown when the requirement is unmet",
          "minLength": 1,
          "title": "Unmet Display Message",
          "type": "string"
        }
      },
      "required": [
        "contract_id",
        "requirement",
        "gate_moment",
        "unmet_display_message"
      ],
      "title": "ModelEvidenceRequirementContract",
      "type": "object"
    },
    "ModelGate": {
      "additionalProperties": false,
      "description": "A gate that requires approval before proceeding.\n\nImmutability:\n    This model uses frozen=True, making instances immutable after creation.\n    This enables safe sharing across threads without synchronization.\n\nStatus Values:\n    Valid: PENDING, APPROVED, REJECTED, SKIPPED, FAILED\n    Invalid: PASSED (gates use approval semantics, not passed/failed)",
      "properties": {
        "id": {
          "description": "Unique identifier for the gate",
          "title": "Id",
          "type": "string"
        },
        "kind": {
          "$ref": "#/$defs/EnumGateKind",
          "description": "Type of gate"
        },
        "description": {
          "description": "Description of what needs approval",
          "title": "Description",
          "type": "string"
        },
        "required": {
          "default": true,
          "description": "Whether approval is required",
          "title": "Required",
          "type": "boolean"
        },
        "status": {
          "$ref": "#/$defs/EnumTicketStepStatus",
          "default": "pending",
          "description": "Current status of the gate"
        },
        "approver": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Who approved/rejected",
          "title": "Approver"
        },
        "decided_at": {
          "anyOf": [
            {
              "format": "date-time",
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "When the decision was made",
          "title": "Decided At"
        }
      },
      "required": [
        "id",
        "kind",
        "description"
      ],
      "title": "ModelGate",
      "type": "object"
    },
    "ModelPermissionContract": {
      "additionalProperties": false,
      "description": "Declares the scopes required to view and to act, plus disabled reasons.\n\n``view_scopes`` gate visibility; ``act_scopes`` gate interaction. When a\nviewer is permitted to see but not act, the action renders disabled and\n``disabled_reason`` states why \u2014 a declared reason is mandatory, never a\nsilent disable.",
      "properties": {
        "permission_id": {
          "description": "Stable semantic identifier for this permission contract",
          "minLength": 1,
          "title": "Permission Id",
          "type": "string"
        },
        "view_scopes": {
          "default": [],
          "description": "Scopes required to see the component; empty means visible to all",
          "items": {
            "type": "string"
          },
          "title": "View Scopes",
          "type": "array"
        },
        "act_scopes": {
          "default": [],
          "description": "Scopes required to act; empty means no extra scope beyond view",
          "items": {
            "type": "string"
          },
          "title": "Act Scopes",
          "type": "array"
        },
        "disabled_reason": {
          "description": "Declared reason shown when the viewer may see but not act",
          "minLength": 1,
          "title": "Disabled Reason",
          "type": "string"
        }
      },
      "required": [
        "permission_id",
        "disabled_reason"
      ],
      "title": "ModelPermissionContract",
      "type": "object"
    },
    "ModelSemVer": {
      "description": "Semantic version model following SemVer 2.0.0 specification.\n\nFull SemVer format: MAJOR.MINOR.PATCH[-prerelease][+build]\n\nPreferred usage (structured format):\n    >>> version = ModelSemVer(major=0, minor=4, patch=0)\n    >>> assert str(version) == \"0.4.0\"\n    >>> assert version.major == 0 and version.minor == 4\n\nWith prerelease and build metadata:\n    >>> version = ModelSemVer(major=1, minor=0, patch=0, prerelease=(\"alpha\", 1))\n    >>> assert str(version) == \"1.0.0-alpha.1\"\n    >>> assert version.is_prerelease() is True\n\nFor parsing external input, use the parse() class method:\n    >>> version = ModelSemVer.parse(\"1.0.0-alpha.1+build.123\")\n    >>> assert version.prerelease == (\"alpha\", 1)\n    >>> assert version.build == (\"build\", \"123\")\n\nPrecedence rules (per SemVer spec):\n    - prerelease < no prerelease (1.0.0-alpha < 1.0.0)\n    - Numeric identifiers < alphanumeric (1.0.0-1 < 1.0.0-alpha)\n    - Build metadata is IGNORED for precedence\n\nNote:\n    String version literals like \"1.0.0\" are deprecated.\n    Always use structured format: ModelSemVer(major=X, minor=Y, patch=Z)\n\n    This model is frozen (immutable) and hashable, suitable for use as dict\n    keys or in sets. Hash is based on major, minor, patch, and prerelease;\n    build metadata is excluded (see __hash__ docstring for details).",
      "properties": {
        "major": {
          "description": "Major version number",
          "minimum": 0,
          "title": "Major",
          "type": "integer"
        },
        "minor": {
          "description": "Minor version number",
          "minimum": 0,
          "title": "Minor",
          "type": "integer"
        },
        "patch": {
          "description": "Patch version number",
          "minimum": 0,
          "title": "Patch",
          "type": "integer"
        },
        "prerelease": {
          "anyOf": [
            {
              "items": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "integer"
                  }
                ]
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Prerelease identifiers (dot-separated in string form)",
          "title": "Prerelease"
        },
        "build": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Build metadata identifiers (ignored for precedence)",
          "title": "Build"
        }
      },
      "required": [
        "major",
        "minor",
        "patch"
      ],
      "title": "ModelSemVer",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "description": "A platform-neutral declaration of a UI component.\n\n``component_kind`` reuses the shipped ``EnumWidgetType`` so a renderer's\nadvertised ``supported_component_kinds`` can gate rendering. ``data_bindings``,\n``actions``, ``evidence_requirements``, and ``permission`` compose the other\nPhase 0 primitives. ``supported_empty_state_reasons`` declares which typed\nreasons this component can surface; the client never blanks silently.",
  "properties": {
    "component_id": {
      "description": "Stable semantic identifier for this component contract",
      "minLength": 1,
      "title": "Component Id",
      "type": "string"
    },
    "component_kind": {
      "$ref": "#/$defs/EnumWidgetType",
      "description": "Shipped component kind a renderer must support to render this"
    },
    "title": {
      "description": "Human-readable component title",
      "minLength": 1,
      "title": "Title",
      "type": "string"
    },
    "contract_version": {
      "$ref": "#/$defs/ModelSemVer",
      "description": "Semantic version of this component contract (additive/versioned)"
    },
    "data_bindings": {
      "default": [],
      "description": "Projection bindings this component reads truth from",
      "items": {
        "$ref": "#/$defs/ModelDataBindingContract"
      },
      "title": "Data Bindings",
      "type": "array"
    },
    "actions": {
      "default": [],
      "description": "Declared command-emitting actions this component exposes",
      "items": {
        "$ref": "#/$defs/ModelActionContract"
      },
      "title": "Actions",
      "type": "array"
    },
    "evidence_requirements": {
      "default": [],
      "description": "Evidence required before this component renders or commits",
      "items": {
        "$ref": "#/$defs/ModelEvidenceRequirementContract"
      },
      "title": "Evidence Requirements",
      "type": "array"
    },
    "permission": {
      "anyOf": [
        {
          "$ref": "#/$defs/ModelPermissionContract"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Permission contract gating who may see/act; None means unrestricted"
    },
    "supported_empty_state_reasons": {
      "default": [],
      "description": "Typed empty-state reasons this component can surface",
      "items": {
        "$ref": "#/$defs/EnumEmptyStateReason"
      },
      "title": "Supported Empty State Reasons",
      "type": "array"
    }
  },
  "required": [
    "component_id",
    "component_kind",
    "title",
    "contract_version"
  ],
  "title": "ModelComponentContract",
  "type": "object"
}
