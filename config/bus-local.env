# Local bus overlay — loaded by npm run dev:local / start:local
# Sets KAFKA_BOOTSTRAP_SERVERS to local Docker Redpanda (port 19092).
# This file is intentionally committed — it contains no secrets.
KAFKA_BOOTSTRAP_SERVERS=localhost:19092
OMNIDASH_BUS_MODE=local
