/**
 * Pattern scoring thresholds for UI color coding
 * Used by ScoringEvidenceCard and potentially other pattern components
 */

/** Score threshold for "good" (green) - 70% and above */
export const SCORE_THRESHOLD_GOOD = 0.7;

/** Score threshold for "moderate" (yellow) - 40% to 70% */
export const SCORE_THRESHOLD_MODERATE = 0.4;
