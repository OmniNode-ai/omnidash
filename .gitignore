node_modules/
dist/
.vite/
*.local
.env.local*
fixtures/
# Storybook fixture builders are part of the source tree (OMN-103).
!src/storybook/fixtures/
!src/storybook/fixtures/**
# OMN-10305: dev server fixtures for E2E proof-of-life (served via /_fixtures middleware).
!fixtures/
!fixtures/**
!server/fixtures/
!server/fixtures/**
build/
dashboard-layouts/

# Skill-result artifacts (local runs; not for repo)
.onex_state/

# Stray compiled JS from .ts sources (pre-existing, should not be in repo)
src/**/*.js
src/**/*.test.js
shared/**/*.js
shared/**/*.test.js

# Prototype design files — captured locally, large, not committed.
# reference/README.md is tracked as a breadcrumb to where the files came from.
reference/claude-design/

*storybook.log
storybook-static

# tsc --build / project references — intermediate cache files
*.tsbuildinfo

# vitest coverage output — generated by `npm run test:coverage`
coverage/

# Additional build/cache outputs
.next/
.turbo/
.DS_Store
